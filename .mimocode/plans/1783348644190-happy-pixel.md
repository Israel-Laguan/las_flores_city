# Milestone 0 Gap Analysis & Milestone 1 Readiness

## Milestone 0 Status

| Task | Status | Notes |
|------|--------|-------|
| 0A — `lore_ref` in YAML schemas | DONE | All 6 schemas in `shared/src/schemas/yaml-content.ts` have `lore_ref: z.string().max(255).optional()` |
| 0B — `/admin/lore` endpoints | DONE | `server/src/routes/admin-lore.ts` (445 lines): `GET /tree`, `GET /file`, `GET /search` all implemented |
| 0C — `/admin/content/file` endpoints | PARTIAL | `PUT /file` done (atomic write with `.tmp` + rename). **Missing: `GET /file` and `GET /tree`** |
| 0D — `/admin/coverage` endpoint | DONE | `server/src/routes/admin-coverage.ts` (575 lines): `GET /admin/coverage` with full cross-reference logic |
| 0E — Expanded prompt catalog | DONE | `server/src/routes/assets.helpers.ts`: `getPromptRoots()` returns 3 dirs (ui-concepts, figures, landmarks). Category labels configured. |

### What's Missing in Milestone 0

1. **`GET /admin/content/file?path=...`** — Read a content YAML file by relative path. Should use existing `validateContentPath()` helper. Not yet in `admin-content.ts`.

2. **`GET /admin/content/tree`** — List all YAML files under `content/` with metadata (path, name, type, size, modifiedAt). Not yet in `admin-content.ts`.

3. **Tests** — No test files exist for admin-lore, admin-coverage, or the new file endpoints. The existing `admin-content-preservation.test.ts` and `admin-content-adminfetch.test.ts` cover older endpoints only.

### Registration

Both routers are already registered in `server/src/index.ts` (lines 27-28). No changes needed there.

---

## Milestone 1 Prerequisites

Phase 1 requires Phase 0 complete. The missing `GET /file` and `GET /tree` endpoints block:
- **1B (Lore Browser)**: Uses `GET /admin/lore/file` (done) but the lore browser also needs content file reading
- **1C (Coverage Dashboard)**: Uses `GET /admin/coverage` (done) but content file tree is useful for linking
- **1D (Asset Linking)**: Needs `GET /admin/content/file` to read and `PUT /admin/content/file` to update YAML with `portrait_url`/`background_url`

The missing `GET /content/file` and `GET /content/tree` are prerequisites for Phase 1D specifically.

---

## Recommended Plan

### Step 1: Add `GET /admin/content/file` to `admin-content.ts`

- Reuse `validateContentPath()` for path validation (already handles traversal, `.yaml` check)
- Read file with `fs.promises.readFile` (utf-8)
- Return `{ success: true, data: { path, content, size, modifiedAt } }`
- Path: `server/src/routes/admin-content.ts` (~20 lines)

### Step 2: Add `GET /admin/content/tree` to `admin-content.ts`

- Walk `resolveContentDir()` recursively
- Filter for `.yaml` files only
- Infer `type` from subdirectory name (characters → character, scenes → scene, etc.)
- Return tree with path, name, type, size, modifiedAt
- Path: `server/src/routes/admin-content.ts` (~40 lines)

### Step 3: Add tests for the new endpoints

- `server/tests/integration/admin-content-file.test.ts` — tests for GET /file, PUT /file, GET /tree
- Cover: valid read, valid write, invalid YAML, path traversal, non-admin rejection
- Path: new test file

### Step 4: Update status table

- Mark Phase 0 as COMPLETE in `docs/development/authoring-mvp/README.md`

### Step 5: Verify

- `npm run lint --workspace=server`
- `npm run build --workspace=server`
- `npm run test --workspace=server` (relevant tests)
- Manual: `GET /admin/content/file?path=characters/char_ana_kim.yaml` returns content
- Manual: `GET /admin/content/tree` returns all YAML files

---

## Files to Modify

| File | Change |
|------|--------|
| `server/src/routes/admin-content.ts` | Add `GET /file` and `GET /tree` endpoints |
| `server/tests/integration/admin-content-file.test.ts` | New: tests for file endpoints |
| `docs/development/authoring-mvp/README.md` | Update Phase 0 status to COMPLETE |

## Files Already Complete (no changes needed)

- `shared/src/schemas/yaml-content.ts` — 0A done
- `server/src/routes/admin-lore.ts` — 0B done
- `server/src/routes/admin-coverage.ts` — 0D done
- `server/src/routes/assets.helpers.ts` — 0E done
- `server/src/index.ts` — routers already registered
