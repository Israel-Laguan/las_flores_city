{
  "name": "las-flores-content",
  "version": "0.1.0",
  "private": true,
  "description": "YAML content files for Las Flores 2077",
  "scripts": {
    "validate": "cd ../server && npm run validate",
    "migrate": "cd ../server && npm run migrate"
  }
}
